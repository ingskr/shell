################if语句用法################
if [ $? -eq 0 ]
then
echo "解压完成"
else
echo "解压过程出错"
fi
#########################################
解压过程中将文件置为空
>> /dev/null
将文件追加
cat >> ~/.bash_profile << EOF
将文件重新写入
cat > ~/.bash_profile << EOF
